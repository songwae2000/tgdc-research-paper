# A domain prior remembers the site it was written against

Short research paper for the TGDC case study, research track. `main.tex` is the
source and compiles to three pages with pdflatex.

The experiment, data pipeline and pre-registration history live in a separate
repository. Every figure in the paper is printed by `python run_all.py` there.
